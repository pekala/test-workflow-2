name: Publish changed packages

on:
  push:
    branches:
      - master

jobs:
  build:
    name: Semantic monorepo release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: pekala/semantic-monorepo@493d093b9847c57433a6f477ff32b21fce8b799c
